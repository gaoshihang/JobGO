1.重载  
  编译时多态，同一个类中同名的方法具有不同的参数列表（注意：不能根据返回类型进行区分）。  
  
2.重写
  运行时多态，子类对父类方法的实现过程进行重新编写，即形参列表、返回值都不能改变。重写有以下规则：  
  （1）参数列表相同。
  （2）返回类型可以不相同，但必须是父类返回值的派生类。
  （3）访问权限不能比父类中更低。
  （4）声明为final的方法不能重写。
  （5）声明为static的方法不能重写，但能再次声明。
  （6）构造方法不能重写。
  
  
  
  
  
