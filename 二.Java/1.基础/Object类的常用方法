1.clone：创建并返回当前对象的一份拷贝。
2.getClass：获取当前运行时对象的Class。  
3.toString：返回对象的字符串表现形式。  
4.finalize：触发垃圾回收器回收实例。  
5.equals：比较两个对象的内存地址是否相等，一般需要重写。  
6.hashCode：返回对象的哈希值。  
7.notify：唤醒一个在此对象监视器上等待的线程。如果有多个线程，只会唤醒一个。  
8.notifyAll：类似于notify，但是唤醒所有线程。  
9.wait：让当前对象等待。
